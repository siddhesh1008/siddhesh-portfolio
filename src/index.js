import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
```

Save it with Ctrl+S.

**Fix 2: Check App.js**

Click on App.js tab at the top. Does it have the portfolio code in it or is it still the default React boilerplate? Tell me what you see.

**Fix 3: Confirm which GitHub account**

In the terminal at the bottom run:
```
git remote -v